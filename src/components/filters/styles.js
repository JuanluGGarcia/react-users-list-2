import styled from 'styled-components';

const StyledFilterContainer = styled.div`
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 1rem;
`;

export { StyledFilterContainer };
